import React from 'react'
import classNames from 'classnames'
import { withRouter } from 'react-router'
const Label = (props) => (
	
		<a className="label nav-item">
			+ New label
		</a>

	
	)
export default withRouter(Label)